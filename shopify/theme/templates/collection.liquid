<div class="wwc-util">
  <h1>{{ collection.title }}</h1>
  {% for product in collection.products %}
    <p><a href="{{ product.url }}">{{ product.title }} — {{ product.price | money }}</a></p>
  {% endfor %}
</div>
